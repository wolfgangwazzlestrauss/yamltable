schema:
  $schema: http://json-schema.org/draft-07/schema#
  description: file system path metadata schema
  type: object
  properties:
    name:
      type: string
      pattern: "^[\\w-]+$"
    description:
      type: string
  required:
    - name
    - description
rows:
  - name: repo
